{{ form_start(form) }}
    {{ form_widget(form) }}
    {% if "ROLE_ADMIN" in app.user.roles %}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {% endif %}
{{ form_end(form) }}
